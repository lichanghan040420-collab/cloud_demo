<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Cargo Management System - Outbound</title>
  <style>
    :root {
      --bg: #0f172a;
      --card: #111827;
      --accent: #3b82f6;
      --accent-2: #10b981;
      --text: #e5e7eb;
      --muted: #9ca3af;
      --border: #1f2937;
      --hover: #2563eb;
      --focus: #93c5fd;
    }
    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif;
      background: radial-gradient(1200px 600px at 15% 10%, #0b1022, var(--bg));
      color: var(--text);
    }
    header {
      padding: 24px 20px;
      border-bottom: 1px solid var(--border);
      position: sticky;
      top: 0;
      backdrop-filter: saturate(150%) blur(6px);
      background: rgba(17, 24, 39, 0.6);
    }
    .brand { display: flex; align-items: center; gap: 12px; }
    .logo {
      width: 36px; height: 36px;
      border-radius: 10px;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      display: grid; place-items: center;
      color: white; font-weight: 700;
      box-shadow: 0 6px 20px rgba(16, 185, 129, 0.25);
    }
    .title { font-size: 20px; font-weight: 700; letter-spacing: 0.2px; }
    .subtitle { font-size: 13px; color: var(--muted); }
    main { max-width: 980px; margin: 28px auto; padding: 0 20px 40px; }
    .hero {
      padding: 24px;
      border: 1px solid var(--border);
      border-radius: 16px;
      background: linear-gradient(180deg, rgba(59,130,246,0.08), transparent);
      margin-bottom: 22px;
    }
    .hero h1 { margin: 0 0 8px; font-size: 28px; line-height: 1.25; }
    .hero p { margin: 0; color: var(--muted); font-size: 14px; }
    .layout { display: flex; gap: 20px; margin-top: 24px; }
    .sidebar {
      width: 200px;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .sidebar a {
      display: block;
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid #28446b;
      color: #e6f0ff;
      background: linear-gradient(180deg, #184071, #16365e);
      text-decoration: none;
      font-weight: 600;
      text-align: center;
    }
    .sidebar a:hover {
      background: linear-gradient(180deg, var(--hover), #1b4db0);
      border-color: #2a5bb8;
      box-shadow: 0 6px 18px rgba(59,130,246,0.35);
    }
    .content {
      flex: 1;
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 20px;
      background: var(--card);
    }
    .field { display: flex; flex-direction: column; gap: 8px; margin-bottom: 16px; }
    .field label { font-size: 13px; color: var(--muted); }
    .input {
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid #28446b;
      background: #0c1324;
      color: var(--text);
    }
    .btn {
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid #28446b;
      color: #e6f0ff;
      background: linear-gradient(180deg, #184071, #16365e);
      cursor: pointer;
      font-weight: 600;
    }
    .btn:hover {
      background: linear-gradient(180deg, var(--hover), #1b4db0);
      border-color: #2a5bb8;
    }
    footer {
      border-top: 1px solid var(--border);
      color: var(--muted);
      font-size: 12px;
      padding: 18px 20px;
      text-align: center;
      margin-top: 28px;
    }
  </style>
</head>
<body>
  <header>
    <div class="brand">
      <div>
        <div class="title">Cargo Management System</div>
        <div class="subtitle">Efficient Inbound | Accurate Outbound | Quick Search | Secure Edit</div>
      </div>
    </div>
  </header>

  <main>
    <section class="hero">
      <h1>Cargo Outbound</h1>
      <p>Delete existing cargo records on this page.</p>
    </section>

    <div class="layout">
      <!-- Sidebar Navigation -->
      <nav class="sidebar">
        <a href="/">Home</a>
        <a href="/inbound">Inbound</a>
        <!-- Current page is Outbound, so no "Outbound" button -->
        <a href="/edit">Edit</a>
        <a href="/search">Search</a>
        <a href="/list">List</a>
      </nav>

      <!-- Outbound Function -->
      <div class="content">
        <form id="outboundForm" action="/outbound" method="POST">
          <div class="field">
            <label for="code">Cargo Code</label>
            <input class="input" id="code" name="code" type="text" required />
          </div>
          <button class="btn" type="submit">Delete Cargo</button>
        </form>

        <% if (typeof message !== 'undefined') { %>
          <p style="margin-top:12px;color:var(--accent);"><%= message %></p>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>
